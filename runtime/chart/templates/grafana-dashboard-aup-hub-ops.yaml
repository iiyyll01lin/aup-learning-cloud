{{- if and .Values.hub.metrics.enabled .Values.monitoring.grafana.dashboard.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-aup-hub-ops
  namespace: {{ .Values.monitoring.namespace }}
  labels:
    grafana_dashboard: "1"
data:
  aup-hub-ops.json: |
{{ .Files.Get "grafana-dashboard-aup-hub-ops.json" | indent 4 }}
{{- end }}
